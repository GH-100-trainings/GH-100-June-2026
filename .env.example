# Copy this file to ".env" and fill in your Azure Maps subscription key.
# The .env file is gitignored so your key is never committed.
AZURE_MAPS_KEY=your-azure-maps-subscription-key

# Optional: override the default port (3000).
# PORT=3000
